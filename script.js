AOS.init({
    duration: 1500, // Animation duration in milliseconds
    offset: 80,    // Offset (in pixels) from the original trigger point
    once: false     // Animation should appear both on scroll up and down
});